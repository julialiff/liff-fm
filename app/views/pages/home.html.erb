<div class="container">
<% if user_signed_in? %>
  <div class="row">
    <div class="col-xs-12">
      <h4>Olá, <b><%= current_user.name %>.</b></h4>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-12">
      <% if current_user.twitter.nil? %>
        <p class="alert alert-danger">Por favor, associe sua conta do Twitter clicando neste link.</p>
      <% end %>
    </div>
  </div>

  <% if current_user.lastfm.nil? %>
    <p class="alert alert-danger">Por favor, associe sua conta do last.fm clicando neste link.</p>
  <% else %>
    Meus artistas mais ouvidos esta semana foram: <%= @print_list %><%= "http://last.fm/user/#{current_user.lastfm}" %>
    <%= button_to "Postar agora", {action: 'new'}, {class: 'btn btn-primary'} %>
  <% end %>
  <% if current_user.twitter && current_user.lastfm %>
    Top artistas mais ouvidos da semana
  <% end %>
<% else %>
  Login/Registrar
<% end %>

</div>
