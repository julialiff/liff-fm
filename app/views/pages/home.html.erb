<div class="container">
<% if user_signed_in? %>
  <div class="row">
    <div class="col-xs-12">
      Olá, <%= current_user.name %>.
    </div>
  </div>

  <% if current_user.twitter.nil? %>
    Por favor, associe sua conta do Twitter clicando neste link.
  <% end %>
  <% if current_user.lastfm.nil? %>
    Por favor, associe sua conta do last.fm clicando neste link.
  <% end %>
  <% if current_user.twitter && current_user.lastfm %>
    Top artistas mais ouvidos da semana
  <% end %>
<% else %>
  Login/Registrar
<% end %>

</div>
